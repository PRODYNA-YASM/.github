apiVersion: v1
kind: Service
metadata:
  name: {{ include "yasm-github.fullname" . }}
  labels:
    {{- include "yasm-github.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yasm-github.selectorLabels" . | nindent 4 }}
