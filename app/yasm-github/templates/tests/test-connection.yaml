apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yasm-github.fullname" . }}-test-connection"
  labels:
    {{- include "yasm-github.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yasm-github.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
